// Test: Foreign signatures (FFI declarations)
// Category: Core - Sig
// Expected: Compiles with sig declarations

sig my_add: Int -> Int;
sig my_check: Int -> Bool;

let result: Int = 0;
let flag: Bool = false;

rct txn test_sig [flag == false]
  [result >= 0]
{
    term;
};