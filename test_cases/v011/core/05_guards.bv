// Test: Guards (conditional statements within transaction)
// Category: Core - Guards
// Expected: Compiles with guarded statements

let flag: Bool = false;
let counter: Int = 0;

rct txn update [counter >= 0]
  [counter == @counter + 1]
{
    &counter = counter + 1;
    term;
};